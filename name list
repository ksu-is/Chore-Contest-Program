names = []

def name_list():
    name=input("Name to add to list of contestants: ")
    while len(name)>1:
        if name == 0:
            break
        else:
            names.append(name)
            name = input("Name to add to list of contestants: ")


print("Please type names as prompted until all the people participating in the contest have been added to the list. When done simply type '0'")
name_list()
print(names)
